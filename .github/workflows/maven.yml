# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Download spigots
        run: |
          rm -f -R lib
          apt-get update
          apt-get install -y wget
          mkdir lib
          wget -O lib/spigot-1.16.1.jar https://cdn.getbukkit.org/spigot/spigot-1.16.1.jar
          wget -O lib/spigot-1.15.2.jar https://cdn.getbukkit.org/spigot/spigot-1.15.2.jar
          wget -O lib/spigot-1.14.4.jar https://cdn.getbukkit.org/spigot/spigot-1.14.4.jar
          wget -O lib/spigot-1.13.2.jar https://cdn.getbukkit.org/spigot/spigot-1.13.2.jar
      - name: Build with Maven
        run: mvn clean install package javadoc:javadoc
      - uses: actions/upload-artifact@v1
        with:
          name: "ItemMods"
          path: "target"
  pages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v1
        with:
          python-version: '3.8.3'
          architecture: 'x64'
      - name: Cache dependencies
        uses: actions/cache@v1
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install -r ./requirements.txt
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Download artifacts
        uses: actions/download-artifact@v2
        with:
          name: "ItemMods"
      - name: Build
        run: |
          mv assets docs
          mv ./*.md docs
          mkdocs build
          mkdir public
          mv site/* public
          mv target/site/* public
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@3.5.8
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages # The branch the action should deploy to.
          FOLDER: public # The folder the action should deploy.
